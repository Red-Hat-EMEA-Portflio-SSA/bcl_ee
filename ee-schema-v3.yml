---
version: 3

dependencies:
  galaxy: requirements.yml
  python: requirements.txt
  system: bindep.txt

images:
  base_image:
    name: registry.redhat.io/ansible-automation-platform-23/ee-minimal-rhel8:1.0.0-294

options:
  package_manager_path: /usr/bin/microdnf

additional_build_files:
  - src: ./ansible.cfg
    dest: configs
  - src: ./vsan-sdk-python.zip
    dest: configs

additional_build_steps:
  prepend_galaxy:
    - COPY _build/configs/ansible.cfg /home/runner/.ansible.cfg
  prepend_final: |
    RUN whoami
    RUN cat /etc/os-release
  append_final:
    - ENV PYTHONPATH=/usr/libexec/platform-python
    - COPY _build/configs/vsan-sdk-python.zip .
    - RUN unzip -q ./vsan-sdk-python.zip -d /tmp
    - RUN cp /tmp/vsan-sdk-python/bindings/vsanmgmtObjects.py /usr/lib/python3.9/site-packages
    - RUN cp /tmp/vsan-sdk-python/samplecode/vsanapiutils.py  /usr/lib/python3.9/site-packages
    - RUN python -c 'import vsanapiutils'


#    - RUN ls /usr/lib/python3.9/site-packages/vsanapiutils.py
#    - RUN ls /usr/lib/python3.9/site-packages/vsanmgmtObjects.py

# python -c 'import sys; import pprint; pprint.pprint(sys.path)'
# /usr/lib/python3.9/site-packages
# /usr/lib64/python3.9/site-packages
# /usr/local/lib/python3.9/site-packages
# /usr/local/lib64/python3.9/site-packages
# /usr/lib64/python3.9/lib-dynload
# /usr/lib64/python3.9

# Execution tells me to put everything here /usr/bin/python3.9

# python -c 'import vsanapiutils'